@model SalesCollectionViewModel
@{
    ViewData["Title"] = "Sales Return List";
}

<div class="row clearfix">
    <div class="col-12">
        <div class="card mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h5 class="m-0 font-weight-bold text-primary">Sales Return List</h5>
            </div>
            <!-- Card Body -->
            <div class="card-body">
                <table class="table table-striped table-bordered" id="departmentTable1">
                    <thead>
                        <tr>
                            <th>Customer Name</th>
                            <th>Return Invoice Number</th>
                            <th>Return Date</th>
                            <th style="text-align:right">Total Return Amount</th>
                            <th>Action</th>
                    </thead>
                    <tbody>
                        @foreach (var data in Model.salesReturnInvoiceMasters)
                        {
                            <tr>
                                <td>@data.relSupplierCustomerResourse.Leads.leadName</td>
                                <td>@data.invoiceNumber</td>
                                <td>@data.invoiceDate?.ToString("dd/MM/yyyy")</td>
                                <td style="text-align:right">@data.totalAmount?.ToString("0,0.00")</td>
                                <td>
                                    <a title="Print Bill Receipt" class="btn btn-info" href="/OtherSales/SalesInvoice/ReturnInvoicePDFAction?id=@data.Id" target="_blank"><i class="fa fa-print"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


@section Scripts{


    <script type="text/javascript">
        $(document).ready(function () {
            $('#departmentTable1').DataTable();


            //var table = $('#PurchaseList').DataTable();

            //$('#PurchaseList tbody').on('click', 'tr', function () {
            //    var data = table.row(this).data();
            //    var iddata = data[1];
            //    let id = iddata.replace('<span style="display:none">', "").replace("</span>", "");
            //    var vid = parseInt(id.split('-')[1]);
            //    window.location.href = "/Purchase/PurchaseOrder/PurchaseDetail/?id=" + vid;
            //});
            //$('#PurchaseList tbody').on('click', 'td:last-child', function (e) {
            //    e.stopPropagation();
            //});

        });
    </script>
}

