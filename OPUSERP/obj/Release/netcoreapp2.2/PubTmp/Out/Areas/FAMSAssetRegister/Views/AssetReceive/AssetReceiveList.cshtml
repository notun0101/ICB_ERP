@model AssetReceiveViewModel
@{
    ViewData["Title"] = "AssetReceiveList";
}

@section Style{
    <style>
        #assetReceiveTable tbody tr {
            cursor: pointer;
        }
    </style>
}

<div class="row clearfix">
    <div class="col-12">
        <div class="card mb-4">
            <!-- Card Header - Dropdown -->
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h4 class="m-0 font-weight-bold text-primary">Asset Receive List</h4>
                <a href="/FAMSAssetRegister/AssetReceive/AssetNotReceiveList" class="btn btn-success btn-sm pull-right">Add Asset Receive</a>
            </div>

            <!-- Card Body -->
            <div class="card-body">
                <table class="table table-striped table-bordered" id="assetReceiveTable">
                    <thead>
                        <tr>
                            <th>Asset No</th>
                            <th>Asset Value</th>
                            <th>Receive Date</th>
                            <th>Receive By</th>
                            @*<th>Action</th>*@
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var data in Model.assetReceives)
                        {
                            <tr>
                                <td><span style="display:none">-@data.Id-</span>@data.assetRegistration.assetNo</td>
                                <td>@data.assetRegistration.assetValue</td>
                                <td>@data.receiveDate?.ToString("dd-MMM-yyyy")</td>
                                <td>@data.receiveBy</td>
                                @*<td>
                                    <a class="btn btn-success" data-toggle="tooltip" title="Edit" href="/FAMSAssetRegister/AssetReceive/AssetReceive/?id=@data.Id&assetId=@data.assetRegistrationId"><i class="fa fa-edit"></i></a>
                                </td>*@
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>

        $(document).ready(function () {

            $('#assetReceiveTable').DataTable();

            //var table = $('#assetReceiveTable').DataTable();
            //$('#assetReceiveTable tbody').on('click', 'tr', function () {
            //    var data = table.row(this).data();
            //    var iddata = data[0];
            //    let id = iddata.replace('<span style="display:none">', "").replace("</span>", "");
            //    var assetid = parseInt(id.split('-')[1]);
            //    window.location.href = "/FAMSAssetRegister/AssetRegister/AssetRegisterDetails/?id=" + assetid;
            //});
            //$('#assetReceiveTable tbody').on('click', 'td:last-child', function (e) {
            //    e.stopPropagation();
            //});
        });

    </script>
}











